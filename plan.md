- Read C:\cTeacher\rules\copilot-rules.md

- Avoid embedding unescaped JS/JSDoc text in one PowerShell string and instead run separate parser-safe checks.

- Do not create any directories or files without my explicit consent. If a file or directory should be created, ask me first, always.

- Make sure that you write the code in a way that makes adding, subtracting, and editing simple and seamless so that adding functionality, buttons, menus, screens, assets and everything else in the program is a simple process.

- Make sure that front-end pipelines are being created often, so that if there's an error it will be extremely simple to find exactly where that error is. I want everything perfectly organized and labeled to avoid any confusion for debugging or adding functionality.

- After each meaningful batch of authorized edits, run only the checks relevant to that batch after their safety and consent gates have passed. Record unavailable, blocked, or unauthorized checks as `not run` or `blocked`; never treat them as passes.

- IMPORTANT Do not run builds, scanners, installations, networked commands, or output-generating validation without explicit consent after disclosing the exact command and side effects.

- IMPORTANT Follow the plan 1 step at a time

- Whenever a new page is created, update the text file names/cmb. Append the page to the bottom of the file while copying the already existing format. Continue enumerating the pages and write the page's name, as it's written in the vue file, next to the word "Page:" and place it in quotation marks just as the pages before it are.

- Whenever a new container is created, update the text file names/cmb. Append the container's name, where it's defined, where it's rendered, and what its function is, in the exact same format as the other containers in the same text file. If the headers and categories needed for the container to be placed under exist then simply fill them out with the relevant information. If either the page header, the category header, or both do not exist then create it and then fill out the relevant information, while following the format seen in the rest of the file.

- Whenever a new button is created, update the text file names/cmb. Append the button's name, where it's defined, where it's rendered, and what its function is, in the exact same format as the other buttons in the same text file. If the headers and categories needed for the button to be placed under exist then simply fill them out with the relevant information. If either the page header, the category header, or both do not exist then create it and then fill out the relevant information, while following the format seen in the rest of the text file.

- Whenever a new menu is created, update the text file names/cmb. Append the menu's name, where it's defined, where it's rendered, and what its function is, in the exact same format as the other menus in the same text file. If the headers and categories needed for the menu to be placed under exist then simply fill them out with the relevant information. If either the page header, the category header, or both do not exist then create it and then fill out the relevant information, while following the format seen in the rest of the text file.

A.) Remove the text "c99" and "Learning Hub" and remove the visible thin, black line that is underlining that text from the c99 panel

B.) Increase the height of the c99 lessons panel to where the top of the panel is 16 pixels away from the top of the c99 panel and the bottom of the c99 lessons panel is 16 pixels away from the bottom of the c99 panel.  Do not move the left edge or the right edge of the c99 lessons panel.

C.) Move the "Close" button in the c99 panel to be in the upper-left hand corner of the c99 panel.

D.) Left-Center align the "Lessons", "Progress", "Reports", and "Games" buttons inside the c99 panel.

E.) Inside the "c99/Lessons" panel create a button for each of the following category names: 

1. Introducing C,   
2. C Fundamentals,  
3. Formatted Input/Output,   
4. Expressions,   
5. Selection Statements,   
6. Loops,   
7. Basic Types,   
8. Arrays,   
9. Functions,   
10. Program Organization,   
11. Pointers,   
12. Pointers and Arrays,   
13. Strings,   
14. The Preprocessor,   
15. Writing Large Programs,   
16. Structures, Unions, and Enumerations,   
17. Advanced Uses of Pointers,   
18. Declarations,   
19. Program Design,   
20. Low-Level Programming,   
21. The Standard Library,   
22. Input/Output,   
23. Library Support for Numbers and Character Data,   
24. Error Handling,   
25. International Features,   
26. Miscellaneous Library Functions, and   
27. Additional C99 Support for Mathematics  

F.) Include the number and period prepending each category name

G.) Create a "close" button for all of the category button menus and place each "close" button in the upper left-hand corner of each individual menu, but do not apply a general rule that applies to them all.  Keep every "close" button and its formatting and styling separate.

H.) When any of the category buttons are clicked, the panel associated with that button should replace the c99 lessons panel as long as it's activated 

I.) Create a separate panel for each individual button.  Give each individual panel the same formatting (size, shape, coloring, etc ...), but do not apply a general rule that applies to them all.  Keep every panel and its formatting and styling separate.

J.) Use the following font: <link rel="stylesheet" href="https://use.typekit.net/vaq7nuy.css"> for the first word on every button. Use the following font: <link rel="stylesheet" href="https://use.typekit.net/vaq7nuy.css"> for every word after the first on each individual button 

K.) If the category name has the word "The" in it, ignore the word "The".  Do not place it on the button or anywhere in the code referring to it.

L.) Ignore all commas, do not place a comma on any button or anywhere in the code referring to it with the following exception: "16. Structures, Unions, and Enumeration", but in this exception, do omit the 1 comman that follows "Enumeration".  Do not omit the commas inbetween the words "Structures" and "Unions" and the comman in between "Unions" and "Enumeration"

M.) Capitalize every word on the buttons except for the following: "and", "of", and "for"

N.) In "26. Miscellaneous Library Functions, and", omit the word "and".  Do not place it on the button or anywhere in the code referring to it.

O.) Give each of the category buttons a height of 68 pixels, including a 2 pixel border and center the text on each button both horizontally and vertically.

P.) Only if the full text that belongs on the button doesn't fit in 1 line can you place it on 2 lines, but do not use 3 lines and do not use 2 lines if the text will fit comfortably on the button.  Do not cut a word off to fit it on 2 lines.  Make the last word that fits comfortably on the 1st line the last word on that line and then continue with the full next word on the 2nd line




